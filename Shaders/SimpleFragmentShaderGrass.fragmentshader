#version 330 core

// The input variable from the vertex shader (Attention! Same name and same type) 
//in vec4 vertexColor;  

// Output data
out vec4 fragColor;


void main()
{
	//Output color = green
	fragColor = vec4(0.34f, 0.48f, 0.1f, 1.0f);

}
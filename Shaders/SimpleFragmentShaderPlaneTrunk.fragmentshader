#version 330 core

// The input variable from the vertex shader (Attention! Same name and same type) 
//in vec4 vertexColor;  

// Output data
out vec4 fragColor;


void main()
{
	//Output color = brown
	fragColor = vec4(0.22f, 0.18f, 0.03f, 1.0f);

}